apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: exercises
  name: app-storage-claim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi