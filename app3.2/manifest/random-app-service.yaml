apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: random-service-svc
spec:
  type: NodePort
  selector:
    app: random-server
  ports:
    - port: 2345
      protocol: TCP
      targetPort: 3000
