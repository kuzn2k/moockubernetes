apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: exercises
  name: random-server-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: random-service-svc
            port:
              number: 2345