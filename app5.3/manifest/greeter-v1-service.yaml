apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: greeter-v1-svc
spec:
  type: ClusterIP
  selector:
    app: greeter-v1
  ports:
    - port: 2345
      protocol: TCP
      targetPort: greeter-port
